# -ServerTools_Helper
Use Discord to safely manage ServerTools XML files. Load versioned configs, edit working copies, import and export XML with automatic backups, validate file integrity, and generate language-specific Phrases.xml outputs without modifying the original source files.
